import styled from 'styled-components'

export const Wrapper = styled.div`
  .ranking-list{
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
  } 

  .pagination {
    margin: 30px 0;
  }
`